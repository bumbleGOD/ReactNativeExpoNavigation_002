import React, { Component } from "react";
import { Text, View } from "react-native";

export const Apply = () => {
   return (
      <View>
         <Text> Apply </Text>
      </View>
   );
};

export default Apply;